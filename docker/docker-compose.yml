services:
  dev:
    build:
      context: ..
      dockerfile: docker/dev.dockerfile
    volumes:
      - ..:/project
    ports:
      - "8888:8888"
      - "5000:5000"
    environment:
      - PYTHONPATH=/workspace
    command: sleep infinity