services:
  dev:
    build: .
    volumes:
      - ./src:/workspace/src
      - ./data:/data
      - ./experiments:/experiments
    ports:
      - "8888:8888"
      - "5000:5000"
    environment:
      - PYTHONPATH=/workspace
    command: sleep infinity